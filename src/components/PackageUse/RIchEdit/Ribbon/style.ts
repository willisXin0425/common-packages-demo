import tw, { styled } from 'twin.macro';

export const Container = styled.div`
  ${tw`relative bg-black`}
`;

export const Content = styled.div`
  ${tw`max-w-[1200px] m-auto flex justify-between`}
`;
